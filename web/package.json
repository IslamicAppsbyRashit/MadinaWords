{
  "name": "madinawords-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MadinaWords — Telegram Mini App для изучения слов мединского курса (фронтенд)",
  "scripts": {
    "predev": "node ../scripts/build-dict.mjs",
    "dev": "vite",
    "prebuild": "node ../scripts/build-dict.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
