{
  "name": "madinawords-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MadinaWords — бэкенд (Fastify + SQLite): авторизация Telegram + синхронизация прогресса",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@fastify/static": "^8.0.0",
    "better-sqlite3": "^11.8.0",
    "fastify": "^5.2.0"
  }
}
